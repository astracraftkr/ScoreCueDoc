title: ScoreCue 문서
description: ScoreCue 악보 뷰어의 약관·정책·라이선스·사용 안내
baseurl: "/ScoreCueDoc"
url: "https://starman902.github.io"
lang: ko

# 문서 페이지는 모두 같은 레이아웃을 쓴다.
defaults:
  - scope:
      path: ""
    values:
      layout: default

exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - vendor
